import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    footer: {
        position: 'absolute',
        bottom: 16,
        fontSize: 8,
        color: '#000',
        fontWeight: '700',
        fontFamily: 'Inter',
        marginBottom: 16,
      },
});