import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
      backgroundColor: '#FFF',
      borderWidth: 1,
      borderColor: '#000',
      borderRadius: 5,
      paddingVertical: 12,
      alignItems: 'center',
      maxWidth: 200,
    },
    buttonText: {
      fontSize: 10,
      color: '#000',
      fontWeight: '700',
    },
  });